/**
 * @klinechart-quant/core/types — canonical shared types.
 *
 * Long-term home for cross-cutting type definitions that several
 * subsystems consume. Today it ships {@link Bar} + its subset helpers;
 * future additions will live alongside.
 */

export type { Bar, ClosePrice, HighLowBar, AnchoredBar } from './bar'
